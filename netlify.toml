[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/docs/*"
  [headers.values]
    Content-Disposition = "inline"
    X-Content-Type-Options = "nosniff"
